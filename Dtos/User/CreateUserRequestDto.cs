using System;
using System.Collections.Generic;
using System.ComponentModel.DataAnnotations;
using System.Linq;
using System.Threading.Tasks;

namespace KimsNebbyShopServer.Dtos.User
{
    public class CreateUserRequestDto
    {
        // [Required]
        // [EmailAddress(ErrorMessage = "Invalid Email format")]
        // public string Email { get; set; }  = string.Empty;

        // [Required]
        // [MinLength(5, ErrorMessage = "Username should be at least 5 characters")]
        // public string Username { get; set; }  = string.Empty;

        // [Required]
        // [MinLength(5, ErrorMessage = "Password must be at least 5 characters")]
        // [MaxLength(30, ErrorMessage = "Password must be at most 30 characters")]
        // public string Password { get; set; }  = string.Empty;

        // [Required]
        // [MaxLength(50, ErrorMessage = "First names must be at most 50 characters")]
        // public string FirstName { get; set; } = string.Empty;
        
        // [Required]
        // [MaxLength(50, ErrorMessage = "Last names must be at most 50 characters")]
        // public string LastName { get; set; }  = string.Empty;
        // public string CardNumber { get; set; } = string.Empty;
    }
}